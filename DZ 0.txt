package Task;

//«адача: необходимо проверить две строки(String) на то, что они €вл€ютс€ анаграммами.
//        јнаграмма - это когда две строки содержат набор из одних и тех же символов.
//        Ќапример УabcdФ и УdabcФ, УlistenФ и УsilentФ, УtriangleФ и УintegralФ - анаграммы.
//        “.е. необходимо создать метод isAnagram("cat", "actФ), который вернет true или false.
//        Ч
//        ѕри разработке алгоритма желетельно стремитьс€ к сложности 0(n)
//        , и к 0(1) по пам€ти.


import javax.xml.transform.SourceLocator;
import java.util.Arrays;

public class Anagram {


    public static void main(String[] args) {
        String word1 = "cat";
        String word2 = "atc";
        isAnagram(word1, word2);
    }

    public static void isAnagram(String a, String b) {

        char[] stingA = a.toCharArray();
        char[] stringB = b.toCharArray();

        for (int i = 0; i < stingA.length; i++) {
        }

        for (int i = 0; i < stringB.length; i++) {
        }

        Arrays.sort(stingA);
        Arrays.sort(stringB);

        if (Arrays.equals(stingA, stringB)) {
            System.out.println(" true ");
        } else {
            System.out.println(" false ");
        }
    }
}